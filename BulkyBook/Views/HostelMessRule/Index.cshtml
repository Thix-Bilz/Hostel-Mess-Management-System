@model IEnumerable<HostelMessRule>
@{
    
}

<br /><br /><br /><br />

    <section class="section center-section">
        <div class="row">
            <div class="col-lg-5"><img src="/assets/img/rule.png" width="750" height="750"></div>
            <div class="col-lg-6">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Namaz Timing</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="HostelMessRule" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i>&nbsp; Create New Data
            </a>
        </div>
    </div>
    <br /><br />

<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Rules
            </th>
            <th>
                Edit & Delete
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model)
       {
           <tr>
               <td>
                   @obj.singleRule
               </td>
               <td>
                   <div class="w-75 btn-group" role="group">
                       <a asp-controller="HostelMessRule" asp-action="Edit" asp-route-id="@obj.id"
                       class="btn btn-primary mx-2"><i class="bi bi-pencil-square"></i></a>
                       <a asp-controller="HostelMessRule" asp-action="Delete" asp-route-id="@obj.id"
                       class="btn btn-danger mx-2"><i class="bi bi-trash-fill"></i></a>
                   </div>
               </td>
           </tr>
       }
    </tbody>
</table>
</div></div>
</section>
